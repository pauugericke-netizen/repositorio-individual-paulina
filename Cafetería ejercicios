import pandas as pd

# Crear los datos
data = {
    "id": [1, 2, 3, 4, 5],
    "frecuencia": [
        "1 a 3 veces",
        "4 a 8 veces",
        "Nunca",
        "Más de 8 veces",
        "1 a 3 veces"
    ],
    "prod_cafe": [1, 1, None, 1, 0],
    "prod_comida": [1, 1, None, 0, 1],
    "prod_frias": [0, 1, None, 1, 1],
    "prod_otros": [0, 0, None, 1, 0],
    "atencion": [4, 5, None, 3, 4],
    "pago_efectivo": [1, 0, None, 1, 1],
    "pago_tarjeta": [1, 1, None, 1, 0],
    "pago_transferencia": [0, 0, None, 1, 1],
    "estudiante": [True, True, False, True, False],
    "edad": [21, 23, 35, 20, 29],
    "mejora": [
        "Más variedad de comida",
        "Que haya más mesas",
        "Más opciones saludables",
        "Mejorar los tiempos de atención",
        "Bajar los precios"
    ]
}

# Crear DataFrame
df = pd.DataFrame(data)

# Guardar como CSV
df.to_csv("encuesta_cafeteria.csv", index=False, encoding="utf-8-sig")

# Mostrar la tabla
print(df)
